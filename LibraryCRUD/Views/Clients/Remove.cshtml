@model LibraryCRUD.Models.Client_Book
@{
    ViewData["Title"] = "Retur Book";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Return Book</h1>

<h3>Are you sure you want to return this book?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Book.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Book.Name)
        </dd>
    </dl>

    <form asp-action="Remove">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Return Book" class="btn btn-danger" /> |
        <a asp-action="Details" asp-route-id="@Model.Client.Id">Back to Details</a>
    </form>
</div>